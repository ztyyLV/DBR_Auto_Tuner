# Pinned deps for a plain `pip install -r requirements.txt` checkout.
# The canonical dependency list lives in pyproject.toml -- prefer:
#   pip install -e ".[pdf,dev]"
#
# Either Dynamsoft bundle works; this is the barcode-only one (versioned to
# match the Barcode Reader). If you already have
# dynamsoft-capture-vision-bundle installed, that satisfies the tool too and
# you do not need this line.
dynamsoft-barcode-reader-bundle>=11.0
pillow>=10.0
pypdf>=4.0
