# Pinned deps for a plain `pip install -r requirements.txt` checkout.
# The canonical dependency list lives in pyproject.toml -- prefer:
#   pip install -e ".[pdf,dev]"
dynamsoft-capture-vision-bundle>=11.2
pillow>=10.0
pypdf>=4.0
